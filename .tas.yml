---
framework: jest
postMerge:
  pattern:
    - 'test/unit/loadGetInitialProps.test.ts'
#     - 'test/unit/**/*.test.{js,ts,tsx}'
preMerge:
  pattern:
    - 'test/unit/loadGetInitialProps.test.ts'
preRun:
  command:
    - yarn
postRun:
  command:
    - yarn --version
version: 1.0
