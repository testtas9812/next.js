---
framework: jest
postMerge:
  pattern:
    - 'test/**/*.test.{js,ts,tsx}'
preMerge:
  pattern:
    - 'test/**/*.test.{js,ts,tsx}'
preRun:
  command:
    - yarn
postRun:
  command:
    - yarn --version
version: 1.0
